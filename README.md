Execute `./run.sh` from project foleder.

To see how progress bar removes previous line run: `./run.sh`.
With `-e` option progress bar works fine: `./run.sh -e`. 

`-e` option unset some IntelliJ Terminal environment variables.
